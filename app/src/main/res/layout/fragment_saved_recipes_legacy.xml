<?xml version="1.0" encoding="utf-8"?>
<!--
    Saved Recipes 레거시 화면용 Fragment 레이아웃

    - 이 레이아웃은 RecyclerView의 "컨테이너 역할"만 담당한다.
    - 아이템 구성, 클릭 처리, 데이터 바인딩 등 실제 UI 로직은
      Fragment / Adapter / ViewHolder에서 처리한다.
    - Compose처럼 상태에 따라 UI가 자동으로 갱신되지 않기 때문에,
      화면 구조와 로직을 명확히 분리하는 것이 중요하다.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        레거시 RecyclerView
        - LayoutManager를 XML에 명시하여 화면 구조를 한눈에 파악 가능하게 한다.
        - 스크롤/재사용/바인딩 관리는 Adapter와 ViewHolder 책임이다.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="16dp"
        android:clipToPadding="false"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
