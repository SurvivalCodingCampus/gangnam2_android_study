## 연습문제 2: 리스트 → 상세 화면 네비게이션 테스트

### 🔹1번

```
@app/src/main/java/com/survivalcoding/gangnam2kiandroidstudy/presentation/scre
en/search_recipe/ 
@app/src/main/java/com/survivalcoding/gangnam2kiandroidstudy/presentation/scre
en/ingredient

리스트 → 상세 화면 네비게이션 테스트 하고싶어. 
시나리오는 다음과 같아
1. SearchRecipeScreen - 리스트 화면
2. 특정 아이템 클릭
3. 상세 화면(IngredientScreen)으로 이동
4. ID가 ViewModel에 제대로 전달되었는지 확인

테스트 요구사항도 만족했으면 좋겠어
* Navigation 발생 여부
* 전달된 ID로 상세 데이터가 정상 표시되는지
* 뒤로 가기 시 상태 유지
```

### 🔹2번

```
@app/src/androidTest/java/com/survivalcoding/gangnam2kiandroi
  dstudy/presentation/screen/search_recipe/SearchToIngredientIn
  tegrationTest.kt 
  컴파일 오류 때문인지 테스트 실행하면 빌드 실패하는 문제가 있어. 문제 해결해줘
```

1. `SearchToIngredientIntegrationTest.kt` 수정:
   * rememberNavBackStack 함수의 사용법이 잘못되어 컴파일 오류가
   발생하던 부분을 수정했습니다 (initial 파라미터 제거).
   * NavDisplay에
   entryDecorators(rememberSaveableStateHolderNavEntryDecorator,
   rememberViewModelStoreNavEntryDecorator)를 추가하여 네비게이션이
   정상 동작하도록 수정했습니다.
   * 테스트 안정성을 위해 타임아웃 시간을 늘리고 waitForIdle()을
   추가했습니다.